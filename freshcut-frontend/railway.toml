[build]
builder = "nixpacks"
buildCommand = "rm -rf node_modules package-lock.json && npm cache clean --force && npm install --os=linux --cpu=x64 && npm run build"

[deploy]
startCommand = "npx serve -s dist -l 3000"
restartPolicyType = "always"
